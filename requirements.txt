numpy==2.4.4
matplotlib==3.10.9
pytest>=7
# Tested with Python 3.11.15. numpy's Generator streams are not guaranteed identical across
# numpy versions, so the pinned version is what regenerates results/ byte for byte.
